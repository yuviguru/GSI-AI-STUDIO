/**
 * @gsi/ui — shared UI primitives, design tokens, responsive helpers.
 *
 * shadcn components land here in a future commit. For now this exports
 * responsive tokens used across both apps.
 */

export * from './tokens';
